# Core dependencies
streamlit==1.29.0
face_recognition==1.3.0
dlib==19.24.2
numpy==1.23.5
pillow==9.5.0
opencv-python-headless==4.7.0.72  # Headless version to avoid GUI-related errors

# Additional dependencies for smooth execution
cmake==3.26.4  # Required for compiling dlib
scipy==1.10.1  # Helps with numerical computations
imutils==0.5.4  # Helps with image processing utilities
scikit-image==0.20.0  # Useful for image transformations
